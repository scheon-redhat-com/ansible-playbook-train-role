---

- name : setup lb tier
  hosts : frontends
  vars : 
   GUID : 66cc
  roles : 
    - { name : common } 
    - { name : lb-tier }

- name : setup app tier
  hosts : apps
  roles : 
    - { name : common }
    - { name : app-tier }

- name : setup db-tier
  hosts : appdbs
  roles : 
    - { name : common }
#    - { name : db-tier }
    - { role : geerlingguy.postgresql }
